<Page
    x:Class="SimpleSongsPlayer.Views.SidePages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleSongsPlayer.Views.SidePages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel>
        <Border Background="{ThemeResource SystemControlBackgroundAccentBrush}">
            <Image Source="/Assets/Square310x310Logo.png" Stretch="Uniform"/>
        </Border>

        <StackPanel Margin="0,8">
            <StackPanel.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="0,2"/>
                </Style>
                <Style x:Key="Title_TextBlock_Style" TargetType="TextBlock" BasedOn="{StaticResource SubtitleTextBlockStyle}">
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                </Style>
            </StackPanel.Resources>
            <TextBlock Text="简易歌曲播放器" Style="{StaticResource Title_TextBlock_Style}"/>
            <TextBlock Text="Simple Songs Player" Style="{StaticResource Title_TextBlock_Style}"/>
            <TextBlock>
                <Run x:Uid="/AboutPage/AppVersion_Run"/>
                <Run Text="{x:Bind appVersion}"/>
            </TextBlock>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <StackPanel.Resources>
                <Style TargetType="AppBarButton">
                    <Setter Property="Padding" Value="8"/>
                    <Setter Property="Margin" Value="4,0"/>
                </Style>
            </StackPanel.Resources>

            <AppBarButton x:Name="SendFeedback_AppBarButton" x:Uid="/AboutPage/SendFeedback_AppBarButton" Click="SendFeedback_AppBarButton_OnClick">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Mail"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="GitHub_AppBarButton" x:Uid="/AboutPage/GitHub_AppBarButton" Click="GitHub_AppBarButton_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE12B;"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="OpenLogsFolder_AppBarButton" x:Uid="/AboutPage/OpenLogsFolder_AppBarButton" Click="OpenLogsFolder_AppBarButton_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE2AC;"/>
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton x:Name="Review_AppBarButton" x:Uid="/AboutPage/Review_AppBarButton" Click="Review_AppBarButton_Click">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE209;"/>
                </AppBarButton.Icon>
            </AppBarButton>
        </StackPanel>
    </StackPanel>
</Page>
