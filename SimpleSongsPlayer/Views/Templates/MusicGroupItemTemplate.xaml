<UserControl
    x:Class="SimpleSongsPlayer.Views.Templates.MusicGroupItemTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleSongsPlayer.Views.Templates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Image x:Name="Cover_Image" Width="98" Height="98"/>
        <StackPanel Grid.Row="1">
            <TextBlock Text="{Binding Name}"/>
            <TextBlock HorizontalAlignment="Right" Style="{ThemeResource CaptionTextBlockStyle}">
                <Run Text="{Binding Items.Count}"/>
                <Run Text=" 首歌"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
