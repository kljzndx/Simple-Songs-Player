<local:LyricsPreviewControlBase
    x:Class="SimpleSongsPlayer.Views.Controllers.ScrollLyricsPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleSongsPlayer.Views.Controllers"
    xmlns:dataModel="using:SimpleSongsPlayer.DataModel"
    xmlns:template="using:SimpleSongsPlayer.Views.ItemTemplates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ScrollViewer x:Name="Root_ScrollViewer">
        <ListView x:Name="Main_ListView" ItemsSource="{x:Bind Lyrics, Mode=OneWay}" SelectionMode="Extended">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dataModel:LyricLine">
                    <template:ScrollLyricsPreviewItemTemplate IsSelected="{x:Bind IsSelected, Mode=OneWay}" LyricText="{x:Bind Content, Mode=OneWay}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </ScrollViewer>
</local:LyricsPreviewControlBase>
