<UserControl
    x:Class="SimpleSongsPlayer.Views.Controllers.SongInfoShowed"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleSongsPlayer.Views.Controllers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Image Width="96" Height="96" Source="{x:Bind CoverSource, Mode=OneWay}" Stretch="Uniform" Visibility="{x:Bind DoShowCover, Mode=OneWay}"/>

        <StackPanel Grid.Column="1" Padding="12,0" VerticalAlignment="Center">
            <TextBlock Text="{x:Bind Title, Mode=OneWay}" FontWeight="Bold" Margin="0,4" Style="{ThemeResource BodyTextBlockStyle}"/>
            <TextBlock Text="{x:Bind Artist, Mode=OneWay}" Margin="0,4" Style="{ThemeResource BodyTextBlockStyle}"/>
        </StackPanel>

    </Grid>
</UserControl>
